// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ActorId } from "./ActorId";
import type { AppealId } from "./AppealId";
import type { AppealOutcome } from "./AppealOutcome";
import type { ProposalId } from "./ProposalId";

/**
 * A filed appeal. Minimal in the first slice: it is recorded, it is routed to
 * the named body, and it can force a review — but it does not have its own
 * adversarial process yet.
 */
export type Appeal = { id: AppealId, proposal: ProposalId, filedBy: ActorId, filedTick: number, grounds: string, 
/**
 * Route copied from the policy at filing time, so later edits to the
 * policy cannot retroactively change where an appeal was heard.
 */
routeId: string, body: string, outcome: AppealOutcome | null, };

// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BuildingId } from "./BuildingId";
import type { EmployerId } from "./EmployerId";
import type { EmployerKind } from "./EmployerKind";
import type { Money } from "./Money";
import type { ResidentId } from "./ResidentId";

export type Employer = { id: EmployerId, name: string, kind: EmployerKind, building: BuildingId, open: boolean, 
/**
 * Employees, kept sorted so iteration order is deterministic.
 */
workforce: Array<ResidentId>, wageDaily: Money, 
/**
 * Unfilled positions available to job seekers.
 */
vacancies: number, 
/**
 * Wage subsidy currently received per worker, in basis points of wage.
 */
wageSubsidyBp: number, 
/**
 * Tick at which the employer closes, if scheduled.
 */
closesAtTick: number | null, 
/**
 * Tick at which a temporary employer stops operating.
 */
endsAtTick: number | null, };

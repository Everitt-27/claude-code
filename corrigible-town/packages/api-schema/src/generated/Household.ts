// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { HouseholdId } from "./HouseholdId";
import type { HousingStatus } from "./HousingStatus";
import type { HousingUnitId } from "./HousingUnitId";
import type { Money } from "./Money";
import type { NeedsStatus } from "./NeedsStatus";
import type { ResidentId } from "./ResidentId";

export type Household = { id: HouseholdId, members: Array<ResidentId>, unit: HousingUnitId | null, rentMonthly: Money, 
/**
 * Unpaid rent accumulated to date.
 */
arrears: Money, status: HousingStatus, 
/**
 * Consecutive months ending with unpaid rent.
 */
monthsInArrears: number, 
/**
 * Tick at which an eviction notice was served, if one is outstanding.
 */
evictionNoticeTick: number | null, 
/**
 * Tick at which the household was evicted, if it was.
 */
evictedTick: number | null, needs: NeedsStatus, 
/**
 * Declared care responsibilities that make civic service costly.
 * Recorded on the household rather than inferred, so the jury-burden
 * panel can show why somebody was excused.
 */
careConstrained: boolean, };

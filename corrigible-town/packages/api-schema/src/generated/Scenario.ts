// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ModelParams } from "./ModelParams";
import type { PolicyDefinition } from "./PolicyDefinition";
import type { ScheduledEvent } from "./ScheduledEvent";
import type { TownBlueprint } from "./TownBlueprint";

export type Scenario = { id: string, version: number, 
/**
 * Version of the rules the engine applies. Stamped on every event so a
 * stream can be replayed against the rules it was produced under.
 */
rulesetVersion: string, title: string, description: string, 
/**
 * Hexadecimal seed. A string, because a 64-bit integer does not survive a
 * round trip through JSON in a browser.
 */
seed: string, 
/**
 * `YYYY-MM-DD` date of tick 0.
 */
startDate: string, town: TownBlueprint, params: ModelParams, 
/**
 * Policies the player may propose.
 */
policyCatalogue: Array<PolicyDefinition>, timeline: Array<ScheduledEvent>, };

// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ActorId } from "./ActorId";
import type { AppealId } from "./AppealId";
import type { BriefClaimRecord } from "./BriefClaimRecord";
import type { BriefStance } from "./BriefStance";
import type { CashPayment } from "./CashPayment";
import type { CriterionEvaluation } from "./CriterionEvaluation";
import type { Disqualification } from "./Disqualification";
import type { EmployerId } from "./EmployerId";
import type { EmployerPayment } from "./EmployerPayment";
import type { EmployerVacancy } from "./EmployerVacancy";
import type { EmploymentChange } from "./EmploymentChange";
import type { HouseholdId } from "./HouseholdId";
import type { HousingStatus } from "./HousingStatus";
import type { IndicatorSnapshot } from "./IndicatorSnapshot";
import type { JuryId } from "./JuryId";
import type { Metric } from "./Metric";
import type { Money } from "./Money";
import type { NeedsChange } from "./NeedsChange";
import type { PolicyId } from "./PolicyId";
import type { ProposalId } from "./ProposalId";
import type { ReasoningCode } from "./ReasoningCode";
import type { RentCharge } from "./RentCharge";
import type { RentPayment } from "./RentPayment";
import type { ResidentId } from "./ResidentId";
import type { ReviewVerdict } from "./ReviewVerdict";
import type { Route } from "./Route";
import type { RoutingRuleOutcome } from "./RoutingRuleOutcome";
import type { StratumQuota } from "./StratumQuota";
import type { TaxKind } from "./TaxKind";
import type { Transfer } from "./Transfer";
import type { TrustChange } from "./TrustChange";
import type { VoteChoice } from "./VoteChoice";
import type { WagePayment } from "./WagePayment";

/**
 * Everything that can happen in Corrigible Town.
 */
export type EventPayload = { "type": "simulationInitialized", scenario_id: string, scenario_version: number, ruleset_version: string, 
/**
 * Hex, because a u64 seed does not survive a round trip through a
 * JavaScript `number`.
 */
seed_hex: string, residents: number, households: number, employers: number, start_date: string, } | { "type": "timeAdvanced", tick: number, date: string, } | { "type": "simulationPaused" } | { "type": "simulationResumed" } | { "type": "speedChanged", days_per_second: number, } | { "type": "employerRevenueReceived", payments: Array<EmployerPayment>, total: Money, } | { "type": "wagesPaid", payments: Array<WagePayment>, total_gross: Money, total_tax: Money, } | { "type": "rentCharged", charges: Array<RentCharge>, total: Money, } | { "type": "rentCollected", payments: Array<RentPayment>, total_paid: Money, total_shortfall: Money, } | { "type": "essentialsPurchased", payments: Array<CashPayment>, total: Money, } | { "type": "discretionarySpending", payments: Array<CashPayment>, total: Money, } | { "type": "propertyTaxCollected", amount: Money, } | { "type": "municipalOperatingCostPaid", amount: Money, } | { "type": "shelterOperatingCostPaid", amount: Money, occupied_beds: number, } | { "type": "municipalBorrowed", amount: Money, reason: string, } | { "type": "unemploymentBenefitPaid", payments: Array<CashPayment>, total: Money, recipients: number, } | { "type": "factoryClosed", employer: EmployerId, employer_name: string, workers_affected: number, reason: string, 
/**
 * Index of the scenario timeline entry that fired, so replay knows the
 * entry has been consumed.
 */
timeline_index: number, } | { "type": "residentLostJob", resident: ResidentId, employer: EmployerId, employer_name: string, previous_wage_daily: Money, } | { "type": "residentFoundJob", resident: ResidentId, employer: EmployerId, employer_name: string, wage_daily: Money, days_unemployed: number, } | { "type": "jobSearchFailed", residents: Array<ResidentId>, vacancies_available: number, } | { "type": "employmentStatusChanged", changes: Array<EmploymentChange>, } | { "type": "vacanciesUpdated", vacancies: Array<EmployerVacancy>, total: number, } | { "type": "rentArrearsIncreased", household: HouseholdId, added: Money, total_arrears: Money, months_in_arrears: number, } | { "type": "housingRiskDetected", household: HouseholdId, arrears: Money, rent_burden_bp: number, threshold_bp: number, } | { "type": "evictionNoticeServed", household: HouseholdId, arrears: Money, deadline_tick: number, appeal_route: string, } | { "type": "evictionOccurred", household: HouseholdId, residents: Array<ResidentId>, arrears: Money, placed_in_shelter: boolean, } | { "type": "shelterPlacementDenied", household: HouseholdId, capacity: number, occupied: number, } | { "type": "shelterCapacityChanged", 
/**
 * The policy responsible, when the change came from one.
 */
proposal: ProposalId | null, additional_capacity: number, capacity_after: number, until_tick: number | null, } | { "type": "householdRehoused", household: HouseholdId, arrears_cleared: Money, } | { "type": "housingStatusChanged", household: HouseholdId, before: HousingStatus, after: HousingStatus, } | { "type": "trustChanged", changes: Array<TrustChange>, reason: string, } | { "type": "needsStatusChanged", changes: Array<NeedsChange>, } | { "type": "indicatorsMeasured", snapshot: IndicatorSnapshot, publish_at_tick: number, } | { "type": "indicatorsPublished", snapshot: IndicatorSnapshot, lag_days: number, } | { "type": "hardshipDetected", indicator: Metric, observed: number, threshold: number, as_of_tick: number, summary: string, } | { "type": "proposalSubmitted", proposal: ProposalId, policy: PolicyId, policy_version: number, title: string, submitted_by: ActorId, } | { "type": "decisionClassified", proposal: ProposalId, route: Route, summary: string, rules: Array<RoutingRuleOutcome>, } | { "type": "publicNoticePosted", proposal: ProposalId, notice_days: number, closes_tick: number, } | { "type": "civicJurySelected", jury: JuryId, proposal: ProposalId, seats: number, invited: Array<ResidentId>, 
/**
 * The seat the player controls. The first slice always summons the
 * player so the civic-jury flow is playable end to end; see
 * `docs/limitations.md`.
 */
player_seat: ResidentId | null, reserves: Array<ResidentId>, strata: Array<StratumQuota>, disqualified: Array<Disqualification>, eligible_pool: number, } | { "type": "juryServiceAccepted", jury: JuryId, resident: ResidentId, lost_work_hours: number, } | { "type": "juryServiceDeclined", jury: JuryId, resident: ResidentId, reason: string, replacement: ResidentId | null, } | { "type": "juryEmpanelled", jury: JuryId, proposal: ProposalId, seated: number, declined: number, } | { "type": "evidenceBriefPublished", jury: JuryId, proposal: ProposalId, brief_id: string, stance: BriefStance, title: string, author_institution: string, summary: string, claims: Array<BriefClaimRecord>, } | { "type": "juryVoteCast", jury: JuryId, juror: ResidentId, choice: VoteChoice, reasoning: Array<ReasoningCode>, score_total: number | null, cast_by_player: boolean, } | { "type": "juryDecisionRecorded", jury: JuryId, proposal: ProposalId, approved: boolean, approve_votes: number, reject_votes: number, abstentions: number, majority_reasoning: string, minority_report: string, } | { "type": "juryCompensationPaid", jury: JuryId, payments: Array<CashPayment>, total: Money, } | { "type": "councilVoteRecorded", proposal: ProposalId, in_favour: number, against: number, abstained: number, passed: boolean, rationale: string, } | { "type": "proposalRejected", proposal: ProposalId, stage: string, reason: string, } | { "type": "policyEnacted", proposal: ProposalId, policy: PolicyId, policy_version: number, title: string, effective_tick: number, review_tick: number, expiry_tick: number | null, funding_source: string, appeal_route: string, } | { "type": "policyImplementationStarted", proposal: ProposalId, delay_days: number, } | { "type": "policyBecameActive", proposal: ProposalId, } | { "type": "benefitPaid", proposal: ProposalId, policy: PolicyId, payments: Array<CashPayment>, total: Money, recipients: number, } | { "type": "wageSubsidyPaid", proposal: ProposalId, policy: PolicyId, payments: Array<EmployerPayment>, total: Money, jobs_supported: number, 
/**
 * Jobs the model judges would have survived anyway. The honest cost of
 * a subsidy: some of it is paid for nothing.
 */
estimated_deadweight_jobs: number, } | { "type": "temporaryJobsCreated", proposal: ProposalId, employer: EmployerId, employer_name: string, count: number, wage_daily: Money, ends_tick: number, } | { "type": "temporaryProgramEnded", employer: EmployerId, employer_name: string, workers_released: Array<ResidentId>, } | { "type": "policyFundingShortfall", proposal: ProposalId, requested: Money, available: Money, } | { "type": "disclosurePublished", proposal: ProposalId, subject: string, snapshot: IndicatorSnapshot, } | { "type": "policyReviewTriggered", proposal: ProposalId, scheduled_tick: number, triggered_early_by_appeal: boolean, } | { "type": "successCriterionMet", proposal: ProposalId, evaluation: CriterionEvaluation, } | { "type": "successCriterionMissed", proposal: ProposalId, evaluation: CriterionEvaluation, } | { "type": "failureCriterionMet", proposal: ProposalId, evaluation: CriterionEvaluation, } | { "type": "policyReviewCompleted", proposal: ProposalId, verdict: ReviewVerdict, success_met: number, success_total: number, failure_met: number, narrative: string, } | { "type": "policyRepealed", proposal: ProposalId, reason: string, } | { "type": "policyExpired", proposal: ProposalId, } | { "type": "appealFiled", appeal: AppealId, proposal: ProposalId, filed_by: ActorId, grounds: string, body: string, deadline_days: number, } | { "type": "appealDecided", appeal: AppealId, proposal: ProposalId, upheld: boolean, reasoning: string, triggered_review: boolean, } | { "type": "taxRateChanged", proposal: ProposalId, tax: TaxKind, rate_bp: number, previous_bp: number, } | { "type": "ledgerPosted", transfers: Array<Transfer>, note: string, };

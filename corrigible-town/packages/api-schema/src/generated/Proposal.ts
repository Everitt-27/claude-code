// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ActorId } from "./ActorId";
import type { Appeal } from "./Appeal";
import type { CouncilVoteRecord } from "./CouncilVoteRecord";
import type { JuryId } from "./JuryId";
import type { Money } from "./Money";
import type { PolicyDefinition } from "./PolicyDefinition";
import type { ProcessStage } from "./ProcessStage";
import type { ProposalId } from "./ProposalId";
import type { ReviewOutcome } from "./ReviewOutcome";
import type { RoutingDecision } from "./RoutingDecision";

/**
 * A proposal working its way through the town's institutions.
 */
export type Proposal = { id: ProposalId, policy: PolicyDefinition, submittedBy: ActorId, submittedTick: number, stage: ProcessStage, 
/**
 * Tick at which the proposal entered its current stage. Process timers
 * (notice periods, briefing time, deliberation) are measured from here.
 */
stageEnteredTick: number, 
/**
 * `None` until the clerk classifies it.
 */
routing: RoutingDecision | null, noticePostedTick: number | null, jury: JuryId | null, councilVote: CouncilVoteRecord | null, enactedTick: number | null, 
/**
 * Tick at which effects start reaching residents.
 */
effectiveTick: number | null, reviewTick: number | null, expiryTick: number | null, reviewOutcome: ReviewOutcome | null, appeals: Array<Appeal>, 
/**
 * Money actually disbursed under this policy so far.
 */
spendToDate: Money, 
/**
 * Residents who have received something under this policy.
 */
beneficiaries: number, };

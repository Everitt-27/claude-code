// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AgeCohort } from "./AgeCohort";
import type { ConflictTag } from "./ConflictTag";
import type { DistrictId } from "./DistrictId";
import type { EmployerId } from "./EmployerId";
import type { EmploymentStatus } from "./EmploymentStatus";
import type { HouseholdId } from "./HouseholdId";
import type { HousingUnitId } from "./HousingUnitId";
import type { Money } from "./Money";
import type { NeedsStatus } from "./NeedsStatus";
import type { Point } from "./Point";
import type { ResidentId } from "./ResidentId";

/**
 * One simulated person.
 */
export type Resident = { id: ResidentId, 
/**
 * Display name, generated deterministically from the seed.
 */
name: string, ageCohort: AgeCohort, household: HouseholdId, employmentStatus: EmploymentStatus, employer: EmployerId | null, 
/**
 * Gross daily wage while employed.
 */
incomeDaily: Money, 
/**
 * Tick at which this resident last became unemployed. `None` while in
 * work. Stored as a tick rather than a running counter so that nothing has
 * to mutate the resident every single day — every state change in the
 * model corresponds to an event, and "another day passed" is not one.
 */
unemployedSinceTick: number | null, 
/**
 * Trust in municipal government, in basis points (0..=10_000).
 */
trustBp: number, 
/**
 * Propensity to accept civic duties, in basis points (0..=10_000).
 */
civicInclinationBp: number, 
/**
 * Risk aversion, used by the transparent juror scoring model.
 */
riskAversionBp: number, needs: NeedsStatus, home: HousingUnitId | null, homeLocation: Point, location: Point, district: DistrictId, conflicts: Array<ConflictTag>, 
/**
 * Hours of paid work lost to civic service so far.
 */
civicHoursServed: number, 
/**
 * True when this resident is the seat the player controls, if any.
 */
playerControlled: boolean, };
